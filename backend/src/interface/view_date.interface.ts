// import { Post } from './post.interface';
// import { User } from './user.interface';

export interface ViewDate {
  id: number;
  last_view: Date;
  writer_id: number;
  post_id: number;
  // Writer: User;
  // Post: Post;
}
